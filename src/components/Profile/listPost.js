import {React,useState,useEffect} from 'react';
import './Profile.css';
import Post from '../Home/CenterTab/Post';
import { CenterTab } from '../Home/CenterTab';
export default function listPost({userData}) {
  //   const [posts, setPosts] = useState([]);

  //   useEffect(() => {
  //     const fetchData = async () => {
        
  //     };
  
  //     fetchData();
  //   }, [])
  
  //   return (
  //     posts.map((item) => <Post key={item.pid} post={item} />)
  // )
  // const [posts, setPosts] = useState([]);

  //   useEffect(() => {
  //     const fetchData = async () => {
  //       setPosts(await getPost(userData.uid));
  //     };

  //     fetchData();
  //   }, [])
  // return (<CenterTab />);
}
